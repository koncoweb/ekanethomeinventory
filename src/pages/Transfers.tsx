import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

const Transfers = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Inventory Transfers</CardTitle>
        <CardDescription>Here you can manage inventory transfers between branches.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Inventory transfer functionality will be implemented here.</p>
      </CardContent>
    </Card>
  );
};
export default Transfers;